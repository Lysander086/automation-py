from pathlib import Path
import os
# the / operator that we normally use for division can also combine Path objects and strings
tPath = Path('spam') / 'bacon' / 'eggs' 


os.chdir('C:\\Windows\\System32')
